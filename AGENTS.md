# AGENTS.md — Learn Career Kit

## 项目概述

开源的「个人学习 + 职业决策知识库」框架：用 AI Agent（Claude Code / Codex 等支持 AGENTS.md 的工具）驱动 **能力评估 → 目标岗位差距 → 系统学习 → 知识沉淀 → 再评估** 的成长闭环。所有示例数据为虚构人物「张三」，仅供体例演示。

核心设计见 `docs/SPEC.md`。

## 目录结构

```
docs/
  SPEC.md                       # 产品定位/核心闭环/模块表（先读这个）
  能力评估数据.json             # 能力评分唯一真相源（维度+技能+岗位+snapshots）
  个人能力评分表.md             # 脚本生成（勿手改）
  能力评估报告.html             # 脚本生成（雷达图+趋势折线，浏览器打开）
  能力评估趋势.csv              # 脚本生成（Excel 看进步）
  岗位能力图谱-严格版.md        # 岗位能力要求权威标准（主骨架，对齐技能 ID）
  前端架构师知识图谱.md         # 前端深度教材
  后端高级架构师能力图谱.md     # 后端架构师详解
  系统设计教材*.md / 微服务面试题集 / 后端八股与架构师面试教材 等  # 知识库教材
  templates/                    # 工作文件模板（生词本/错题本/学习记录/每日日志/技术学习清单）
                                # 首次使用复制到 docs/ 同名位置
面试题/                         # 面试题 md + 专用导出样式
scripts/                        # 评估报告 + md/pdf 转换脚本
.claude/commands/               # /study /skill-test 命令（Agent 驱动学习入口）
assets/                         # md2pdf 样式与可选字体
```

## 📚 文档职责矩阵（避免重复写入）

> 5 类内容的「唯一写入地」硬规则。

| 内容类型 | 唯一写入地 | 禁止在哪重复 | 字段规范 |
|----------|------------|--------------|----------|
| **错题**（答错/不完整的题） | `docs/错题本.md` | 不要在日志/学习记录里展开完整错题，只写「详见错题本 X 类」 | 题目/错误答案/正确答案/原因/掌握标志 |
| **已学主题档案** | `docs/学习记录.md` | 一个主题一条；禁止内联完整错题清单；每条只一句薄弱点 | 方向/日期/主题/状态🟠🟡🟢/评分/技能key/薄弱点一句/下一步 |
| **会话过程流水** | `docs/每日学习日志.md` | 测验分数、面试每轮评价、本次评分变更；错题以「详见错题本 X 类」引用 | 来源/主题/测验/面试每轮/评分更新/下一步 |
| **待办/缺口** | `docs/技术学习清单.md` | 唯一待办池；状态变 🟠/🟢 后必须迁到「已完成」区并链到学习记录 | 项目/状态/范围/来源/备注 |
| **评分数字** | `docs/能力评估数据.json` | 唯一真相源；评分表/HTML/CSV 全是脚本转写，勿手改 | snapshots 数组每条 `{date, source, scope, scores:{"dim.skill":{score, note}}}` |

## 📊 数据流向图

```
                         ┌─────────────────────┐
                         │ 能力评估数据.json     │ ◄── 唯一真相源
                         │ (snapshots[] 数组)   │
                         └──────────┬──────────┘
                                    │ node scripts/gen-skill-report.mjs
              ┌─────────────────────┼─────────────────────┐
              ▼                     ▼                     ▼
   ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
   │ 个人能力评分表.md │  │ 能力评估报告.html│  │ 能力评估趋势.csv │
   │ (脚本生成/勿手改)│  │ (脚本生成)       │  │ (脚本生成)       │
   └──────────────────┘  └──────────────────┘  └──────────────────┘

   命令写入路径：
   /study      ──► snapshots + 学习记录 + 每日日志 + 技术清单 + 错题本 + 生词本
   /skill-test ──► snapshots + 错题本（+ 可选 job-gap-report 重算）

   依赖关系（谁读谁）：
   岗位能力图谱-严格版.md ──读──► 能力评估数据.json（技能 ID 对齐）
                        └─读──► 前端/后端架构师知识图谱.md（深度详解）
```

## 能力评估数据

- **唯一真相源**：`docs/能力评估数据.json`（维度/技能/岗位目标/`snapshots` 历史快照数组，每次评估追加一条形成进步趋势）
- `node scripts/gen-skill-report.mjs` 一键生成评分表 + HTML 可视化 + 趋势 CSV
- `node scripts/job-gap-report.mjs` 计算当前能力与目标岗位的差距矩阵（岗位权重在脚本顶部 `JOB_WEIGHTS`，可按自己目标改）
- `/skill-test` 和 `/study` 完成后自动追加 snapshot 并重跑脚本

## 评分体系

百分制：**0=待评估 | 20=不了解 | 40=了解概念 | 60=能使用（达标线）| 80=熟练掌握（竞争力线）| 100=精通/能教别人**

## 学习材料粒度规范（重要，/study 默认遵守）

> 学习材料要写到「用户不需要追问就能理解透彻」的粒度，而非提纲式罗列。

1. **先讲"为什么"**：这个概念要解决什么真实痛点？用具体场景带出，而非直接抛定义
2. **讲透机理**：不只给结论，要讲"怎么做到的"，涉及算法给伪代码/源码级解释
3. **纠正直觉误区**：主动指出常见误解
4. **预判追问并主动深入**：为什么/怎么落地/和 X 有什么区别，一次讲透
5. **给可直接背的面试金句**：核心知识点配 `> 🔑` 金句
6. **反例对比 + 务实建议**：常见错误做法长什么样、为什么错
7. **工业级落地最佳实践**：用什么库/中间件、参数怎么配、典型坑在哪，给可直接照抄的工程方案
8. **面试连环追问**：概念→机理→实现→生产坑的纵深追问链，每条配完整答案
9. **结构顺序**：先讲解（原理+工程化）→ 后追问。禁止上来就抛面试题
10. **抽象概念必须配 ASCII 图**：结构图/流程图/对比图，配文字解释
11. **示例必须用真实业务场景**：贯穿一个业务（推荐电商订单系统），字段名/业务值真实化，禁止 `id=1` 类无意义数字
12. **缩写必须给全拼**：首次出现给 `全拼 + 中文 + 常见别名`

## 快速命令

```bash
pnpm install                    # 安装依赖（puppeteer）
make report                     # 生成能力评估报告（评分表+HTML可视化+趋势CSV）
make gap                        # 岗位能力差距矩阵
make md2pdf f=docs/某文档.md     # Markdown → PDF（精美样式）
make md-pdf-dir d=docs          # 批量导出目录下全部 md → PDF
make pdf2md f=xx.pdf            # PDF → Markdown
make info                       # 查看环境信息
```

## 包管理

- `pnpm` 是唯一包管理器，禁用 npm/yarn

## 信息真实性原则

> 所有个人数据、经历、量化结果必须真实、可验证。示例数据（张三）为虚构，仅演示体例。

技术教材必须事实正确：不确定的概念要么查证，要么标注「待验证」。分布式系统术语（一致性/隔离级别/共识算法/复制策略）必须按学术定义使用，不可口语化。
